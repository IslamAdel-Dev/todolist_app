import 'package:flutter/material.dart';

class TaskTab extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(body:
      Text("Task Tab",textAlign: TextAlign.center,)),
    );
  }
}
